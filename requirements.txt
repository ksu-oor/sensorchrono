# --- core capture + analysis (runtime) ---
matplotlib
numpy
scipy
pyserial          # shimmer bridge: COM/Bluetooth
pylsl             # LSL outlets/inlets (needs liblsl native lib)
pyxdf             # analysis/: read recorded .xdf
pyyaml            # profiles/*.yaml + sensorchrono config.yaml
sounddevice       # audio_lsl_bridge mic capture
pynput            # keyboard_fiducial_bridge global HID hook
opencv-python     # video_lsl_bridge camera capture (imported as cv2)
websockets        # emotiv/cortex bridge

# --- sensorchrono desktop app (GUI, Phase 3) ---
PySide6
pyqtgraph

# --- packaging (dev only, Phase 4) ---
pyinstaller
